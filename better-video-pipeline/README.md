# Usage

## how to run with nix

`nix run github:canavan-a/home-server/main#streamer --no-write-lock-file --refresh`

## otherwise pray you have the correct system installs and run

wd (build)

`cmake --build .`

wd (cpp root)

`./build/main`
